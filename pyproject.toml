[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ome-zarr-tools"
dynamic =["version"]

[project.urls]
Repository = "https://github.com/chourroutm/ome-zarr-tools"
Issues = "https://github.com/chourroutm/ome-zarr-tools/issues"
